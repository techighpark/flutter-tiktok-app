import 'package:flutter/widgets.dart';

//It is suitable when dealing whit only one value.
//ValueNotifier + AnimatedBuilder
//ValueNotifier + ValueListenableBuilder
//ValueNotifier + eventListener
final videoConfigValue = ValueNotifier(false);
